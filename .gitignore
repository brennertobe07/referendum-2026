# Logs and intermediate artifacts (regeneratable)
notes/*log*.txt
analysis/_headline.json

# Backups / large binaries (kept locally, not in git)
handoff/*.zip

# OS / editor cruft
Thumbs.db
desktop.ini
.DS_Store
*~

# Claude Code local settings (machine-specific)
.claude/

# Local render previews (notes)
notes/*.png
notes/*.pdf

# Safety: never commit voter-level targeting files (PII)
dropoff_*.csv
**/dropoff_*.csv
